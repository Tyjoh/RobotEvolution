package com.tjgs.robotevolution.map;

/**
 * Collision type for collision resolution
 * Types define the action that happens when a certain tile is collided with
 */
public enum TileType{
    EMPTY,
    NORMAL,
    LADDER,
    ONEWAY,
}
